/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint: { ignoreDuringBuilds: true },
  // imagens internas/externas — adicionaremos domínios se necessário
}
export default nextConfig
